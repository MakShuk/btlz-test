# Шаблон для выполнения тестового задания

## Описание
Шаблон подготовлен для того, чтобы попробовать сократить трудоемкость выполнения тестового задания.

В шаблоне настоены контейнеры для `postgres` и приложения на `nodejs`.
Для взаимодействия с БД используется `knex.js`.
В контейнере `app` используется `build` для приложения на `ts`, но можно использовать и `js`.

Шаблон не является обязательным!\
Можно использовать как есть или изменять на свой вкус.

Все настройки можно найти в файлах:
- compose.yaml
- dockerfile
- package.json
- tsconfig.json
- src/config/env/env.ts
- src/config/knex/knexfile.ts

## Команды:

Запуск базы данных:
```bash
docker compose up -d --build postgres
```

Для выполнения миграций и сидов не из контейнера:
```bash
npm run knex:dev migrate latest
```

```bash
npm run knex:dev seed run
```
Также можно использовать и остальные команды (`migrate make <name>`,`migrate up`, `migrate down` и т.д.)

Для запуска приложения в режиме разработки:
```bash
npm run dev
```

Запуск проверки самого приложения:
```bash
docker compose up -d --build app
```

Для финальной проверки рекомендую:
```bash
docker compose down --rmi local --volumes
docker compose up --build
```

PS: С наилучшими пожеланиями!

---

## Wildberries Integration

Приложение интегрировано с Wildberries Box Tariffs API для автоматического обновления тарифов на доставку и хранение.

### Получение API токена

1. Войдите в личный кабинет Wildberries для продавцов
2. Перейдите в раздел **Настройки** → **Доступ к API**
3. Создайте новый токен с правами на чтение тарифов
4. Скопируйте полученный токен (он отображается только один раз)

⚠️ **Важно**: Храните токен в безопасности и не публикуйте его в публичных репозиториях.

### Конфигурация переменных окружения

Создайте файл `.env` на основе `example.env` и добавьте следующие переменные:

```bash
# Wildberries API Configuration
WB_API_TOKEN=your_wildberries_api_token_here
WB_API_BASE_URL=https://common-api.wildberries.ru

# Google Sheets Configuration
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project-id",...}
GOOGLE_SHEET_IDS=	1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
DEFAULT_SHEET_NAME=stocks_coefs
GOOGLE_APP_SCOPES=https://www.googleapis.com/auth/spreadsheets
```

**Описание переменных:**

| Переменная | Описание | Пример значения |
|------------|----------|-----------------|
| `WB_API_TOKEN` | API токен из личного кабинета WB (обязательный) | `eyJhbGciOiJIUz...` |
| `WB_API_BASE_URL` | Базовый URL API Wildberries | `https://common-api.wildberries.ru` |
| `GOOGLE_CREDENTIALS_JSON` | JSON с учетными данными сервисного аккаунта Google (обязательный) | `{"type":"service_account",...}` |
| `GOOGLE_SHEET_IDS` | Список ID таблиц Google Sheets через запятую (обязательный) | `1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms` |
| `DEFAULT_SHEET_NAME` | Имя листа по умолчанию для записи данных (обязательный) | `stocks_coefs` |
| `GOOGLE_APP_SCOPES` | Scopes для Google API через запятую (необязательный) | `https://www.googleapis.com/auth/spreadsheets` |
| `LOG_LEVEL` | Уровень логирования (debug, info, warn, error) | `info` |
| `LOGS_TO_FILE` | Сохранять логи в файлы (true/false) | `true` |

#### Настройка Google Sheets API

1. **Создание сервисного аккаунта:**
   - Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
   - Создайте новый проект или выберите существующий
   - В разделе "IAM & Admin" → "Service Accounts" создайте новый сервисный аккаунт
   - Сгенерируйте и скачайте JSON-ключ для сервисного аккаунта

2. **Настройка доступа к таблицам:**
   - Откройте Google Sheet, в который нужно записывать данные
   - Нажмите "Поделиться" → "Добавить людей и группы"
   - Введите email сервисного аккаунта (из JSON-ключа, поле `client_email`)
   - Предоставите права "Редактор"

3. **Получение ID таблицы:**
   - Откройте нужную Google Sheet
   - ID таблицы находится в URL между `/d/` и `/edit`
   - Пример: `https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit`
   - ID: `1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms`

4. **Настройка переменных окружения:**
   ```bash
   # Содержимое скачанного JSON-ключа (в одну строку)
   GOOGLE_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project-id",...}'

   # ID таблиц (можно указать несколько через запятую)
   GOOGLE_SHEET_IDS=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms

   # Имя листа для записи данных
   DEFAULT_SHEET_NAME=stocks_coefs
   ```

### Запуск приложения

#### Режим разработки (Development)

```bash
# 1. Установите зависимости
npm install

# 2. Запустите PostgreSQL
docker compose up -d postgres

# 3. Примените миграции
npm run knex:dev migrate:latest

# 4. Запустите приложение
npm run dev
```

Приложение автоматически:
- Запустит планировщик обновления тарифов (каждый час)
- Откроет HTTP сервер на порту из `APP_PORT` (по умолчанию 5000)
- Начнёт логирование в консоль и файлы в директории `LOGS_DIR`

#### Режим production

```bash
# Полная пересборка и запуск всех сервисов
docker compose down --rmi local --volumes
docker compose up --build

# Или для запуска в фоновом режиме
docker compose up -d --build
```

**Проверка работы:**
```bash
# Просмотр логов приложения
docker compose logs -f app

# Проверка health check
curl http://localhost:5000/health
```

### CLI команда update-tariffs

Для ручного обновления тарифов за конкретную дату используйте CLI команду:

```bash
# В режиме разработки
npm run update-tariffs:dev -- --date 2025-11-12

# В production (внутри контейнера)
docker compose exec app npm run update-tariffs -- --date 2025-11-12
```

**Параметры команды:**

| Параметр | Описание | Формат | Обязательный |
|----------|----------|--------|--------------|
| `--date` | Дата для получения тарифов | `YYYY-MM-DD` | Да |

**Примеры использования:**

```bash
# Обновить тарифы за текущую дату
npm run update-tariffs:dev -- --date $(date +%Y-%m-%d)

# Обновить тарифы за конкретную дату
npm run update-tariffs:dev -- --date 2025-11-12

# Восстановить данные за прошлый месяц
npm run update-tariffs:dev -- --date 2025-10-15
```

### Просмотр логов

#### Логи в режиме разработки

Логи сохраняются в директорию, указанную в `LOGS_DIR` (по умолчанию `./logs`):

```bash
# Все логи
tail -f logs/all.log

# Только ошибки
tail -f logs/errors.log

# Логи API клиента
grep "wildberries-api" logs/all.log

# Логи планировщика
grep "tariffs-scheduler" logs/all.log
```

#### Логи Docker контейнеров

```bash
# Логи приложения
docker compose logs -f app

# Логи PostgreSQL
docker compose logs -f postgres

# Последние 100 строк логов
docker compose logs --tail=100 app

# Логи с временными метками
docker compose logs -t app
```

#### Структура логов

Логи структурированы по категориям:

- **[wildberries-api]** - HTTP запросы к WB API, rate limiting, ошибки
- **[tariffs-updater]** - Процесс обновления тарифов, статистика
- **[tariffs-scheduler]** - Работа планировщика, расписание выполнения
- **[warehouse-service]** - Операции с таблицей складов
- **[tariff-service]** - Операции с таблицей тарифов

### Troubleshooting

#### Ошибка: "401 Unauthorized"

**Причина:** Неверный или истёкший API токен.

**Решение:**
1. Проверьте правильность токена в файле `.env`
2. Убедитесь, что токен не истёк в личном кабинете WB
3. Сгенерируйте новый токен при необходимости
4. Перезапустите приложение после обновления токена:
   ```bash
   docker compose restart app
   ```

#### Ошибка: "429 Too Many Requests"

**Причина:** Превышен лимит запросов к API (60 запросов/минута).

**Решение:**
- Приложение автоматически обработает ошибку через retry с увеличивающейся задержкой
- Убедитесь, что не запущено несколько экземпляров приложения одновременно
- Проверьте логи на наличие избыточных запросов:
  ```bash
  grep "rate limit" logs/all.log
  ```

#### Дубликаты данных в БД

**Причина:** Проблема с constraint `uq_warehouse_date`.

**Решение:**
```sql
-- Подключитесь к БД
docker compose exec postgres psql -U postgres -d postgres

-- Проверьте наличие дубликатов
SELECT warehouse_id, tariff_date, COUNT(*)
FROM box_tariffs
GROUP BY warehouse_id, tariff_date
HAVING COUNT(*) > 1;

-- Проверьте constraint
\d box_tariffs
```

#### Планировщик не запускается

**Причина:** Ошибка в расписании cron или проблема при инициализации.

**Решение:**
1. Проверьте логи при старте приложения:
   ```bash
   docker compose logs app | grep "scheduler"
   ```
2. Убедитесь, что миграции применены:
   ```bash
   npm run knex:dev migrate:status
   ```
3. Проверьте доступность PostgreSQL:
   ```bash
   docker compose exec postgres pg_isready
   ```

#### Ошибки парсинга тарифов

**Причина:** Неожиданный формат данных от API (например, числа с запятой).

**Решение:**
- Приложение автоматически преобразует запятые в точки
- Значение `"-"` конвертируется в `NULL`
- Проверьте логи валидации:
  ```bash
  grep "validation" logs/errors.log
  ```

#### Проверка состояния системы

**Health Check Endpoint:**
```bash
curl http://localhost:5000/health
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "database": "connected",
  "lastUpdate": "2025-11-12T10:00:00Z",
  "scheduler": "running",
  "nextRun": "2025-11-12T11:00:00Z"
}
```

**Проверка данных в БД:**
```sql
-- Последние обновления
SELECT w.warehouse_name, t.tariff_date, t.updated_at
FROM box_tariffs t
JOIN warehouses w ON t.warehouse_id = w.id
ORDER BY t.updated_at DESC
LIMIT 10;

-- Статистика за последнюю неделю
SELECT
    tariff_date,
    COUNT(*) as warehouses_count,
    AVG(box_delivery_base) as avg_delivery
FROM box_tariffs
WHERE tariff_date >= CURRENT_DATE - INTERVAL '7 days'
GROUP BY tariff_date
ORDER BY tariff_date DESC;
```

### Дополнительная документация

Подробная техническая документация доступна в директории `docs/`:

- [`docs/wildberries-integration-plan.md`](docs/wildberries-integration-plan.md) - Полный план интеграции
- [`docs/wildberries-api-usage.md`](docs/wildberries-api-usage.md) - Работа с WB API
- [`docs/tariffs-scheduler.md`](docs/tariffs-scheduler.md) - Документация планировщика
- [`docs/logging-system.md`](docs/logging-system.md) - Система логирования

### Мониторинг

Для production окружения рекомендуется настроить мониторинг следующих метрик:

- ✅ Успешность обновлений тарифов (%)
- ⏱️ Время выполнения обновления
- 📊 Количество обработанных складов
- ⚠️ Частота ошибок API
- 💾 Размер таблиц в БД
- 🔄 Статус планировщика

**Пример проверки через cron:**
```bash
# Добавьте в crontab для ежечасной проверки
0 * * * * curl -f http://localhost:5000/health || echo "Health check failed"
```
